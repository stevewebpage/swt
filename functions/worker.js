export default { async fetch(req) { if (req.method === 'POST') { const data = await req.formData(); const email = 'steve@musicrow.us'; const apiKey = 'YOUR_RESEND_API_KEY'; const subject = 'New Form Submission'; const html = `Name: ${data.get('first_name')} Email: ${data.get('email')}`; const res = await fetch('https://api.resend.com/emails', { method: 'POST', headers: { 'Authorization': `Bearer ${apiKey}`, 'Content-Type': 'application/json' }, body: JSON.stringify({ from: 'forms@musicrow.us', to: email, subject, html }) }); return new Response('Submitted'); } } } 
